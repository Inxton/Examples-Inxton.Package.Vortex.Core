<Window x:Class="Simple.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Simple.Wpf"
        mc:Ignorable="d"
        Title="Simple.Wpf" Height="450" Width="800"
        DataContext="{x:Static local:App.Hans}">
    <Grid DataContext="{Binding prgSimple, Mode=OneWay}">
        <Canvas>
            <TextBlock Canvas.Left="284" TextWrapping="Wrap" Canvas.Top="29" Text="{Binding _counter.Cyclic}" FontSize="48"><Run Text="0"/></TextBlock>
            <CheckBox Content="Counter active" Canvas.Left="245" Canvas.Top="122" IsChecked="{Binding _counterActive.Cyclic}"/>
            <Button Content="Reset counter" Canvas.Left="242" Canvas.Top="156" Width="101" Height="24" Click="Button_Click"/>
        </Canvas>
    </Grid>
</Window>
