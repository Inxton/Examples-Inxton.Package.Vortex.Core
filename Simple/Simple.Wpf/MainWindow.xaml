<Window x:Class="Simple.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Simple.Wpf"
        mc:Ignorable="d"
        Title="Simple.Wpf" Height="450" Width="800"
        DataContext="{x:Static local:App.Hans}">
    <Grid DataContext="{Binding prgSimple, Mode=OneWay}">
        <StackPanel>
            <TextBlock TextWrapping="Wrap" Text="{Binding _counter.Cyclic}" FontSize="48" Margin="10" HorizontalAlignment="Center"></TextBlock>
            <ToggleButton Content="Counter on/off" IsChecked="{Binding _counterActive.Cyclic}" Margin="13,10,11,10" HorizontalAlignment="Center" Height="23" Width="771"/>
            <Button Content="Reset counter" Click="Button_Click" Margin="10" HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</Window>
